# Operating guide — Kawaii Katz

Modelled on the `CLAUDE.md` guides in Legal-Leaf Market and Nicotia Market. It lives in
its own file because `CLAUDE.md` and `AGENTS.md` are regenerated by `next dev` and anything
written there is at the mercy of the framework.

---

## 1. What this is

An affiliate storefront that aggregates kawaii products from eight Shopify vendors and
presents them as one catalogue. Money comes from outbound clicks: every product links to
the vendor's own checkout with `affiliateParam` attached, and some carry a coupon code.
We never take payment.

Unlike the three sister sites this is **not** a hand-written static page — it is Next.js 16
(App Router), React 19, TypeScript and Tailwind v4, originally generated by v0.

---

## 2. Deploy model

`main` → Vercel project `legal-leaf/kawaii-katz` → `www.kawaiikatz.com` (the apex
308-redirects to www). Pushing to `main` deploys. There is no manual step.

Do not infer deployment state from GitHub's API — this project deploys without writing
GitHub Deployment records, so `gh api repos/.../deployments` returns 0 and proves nothing.
Use `vercel ls kawaii-katz`.

Environment variable changes need a **redeploy** to take effect: `vercel redeploy <url>`.
The running deployment keeps the snapshot it was built with.

---

## 3. Curator auth ("Ada mode")

`ADA_PIN` gates it. The flow is: `POST /api/ada-login` verifies the PIN server-side and
returns an httpOnly, sameSite=strict cookie carrying an HMAC-signed expiry; every write to
`/api/exclusions` is authorized by that cookie alone.

**The PIN must never appear in a client component.** It shipped in the public bundle once
already — as a hardcoded constant in `AdaLoginModal.tsx` and `useExclusions.ts` — which
made it readable by any visitor with devtools. `ADA_PIN` also doubles as the HMAC signing
key, so rotating it invalidates every live session, which is the behaviour you want.

With `ADA_PIN` unset, login returns 503. It fails closed on purpose; there is no fallback
value.

---

## 4. `/api/catalog` — the scraper

Fans out across every vendor's `products.json` concurrently, up to 5 pages of 250 each. A
vendor that fails is skipped silently so the others still load.

- `GET /api/catalog` — cached 6h (`revalidate`, plus `s-maxage`)
- `GET /api/catalog?debug` — per-vendor fetch counts and how many rows the filters dropped

Use `?debug` before assuming a vendor is dead: the silent catch means a failed fetch and an
empty catalogue look identical from the outside.

Two filter layers drop adult-model apparel — a text filter, then a coco-ssd image scan
budgeted so a cold build cannot hang. Unscanned items stay; the text filter is the backstop.

---

## 5. Environment variables

| Name | Required | Purpose |
|---|---|---|
| `ADA_PIN` | for curator mode | Gates `/api/ada-login`; unset ⇒ 503 |
| `DATABASE_URL` | for write persistence | Neon Postgres, `store_exclusions` table |

The storefront runs without either: `GET /api/exclusions` catches DB errors and returns an
empty list, so only hiding/restoring products breaks.

`.env*` is gitignored. Set the real values in Vercel.

**Never pipe an env value into `vercel env add` from PowerShell** — it prepends a UTF-8 BOM,
so `"5824"` is stored as `﻿5824` and every correct value is rejected. Use bash:
`printf '5824' | vercel env add ADA_PIN production`.

---

## 6. Verify before you merge

There is no test suite, so verify by hand:

1. `pnpm run check` — typecheck. **Do not rely on `pnpm build`**: `next.config.mjs` sets
   `typescript.ignoreBuildErrors: true`, so a build succeeds with type errors intact.

   `pnpm run lint` is **broken as shipped by v0** — the script calls `eslint .` but eslint
   was never added as a dependency, so it fails with "not recognized". Either install and
   configure eslint or delete the script; right now it is a trap that reports a tooling
   failure as if it were a lint failure.
2. `pnpm dev`, then load `/` — confirm real products render, not an empty grid.
3. `GET /api/catalog?debug` — every vendor `ok: true` with a sane count.
4. Anything touching auth: check no-cookie, valid-cookie, forged-cookie and post-logout.
5. Anything user-facing: desktop **and** mobile widths.

---

## 7. Hard "do not" list

- Do NOT put `ADA_PIN`, or any secret, in a `'use client'` file (§3).
- Do NOT trust `pnpm build` as a typecheck (§6).
- Do NOT edit in v0 any more — it pushes to its own sync branch and will fight `main`.
- Do NOT commit `.env*`, `.vercel`, `node_modules`, `.next`.
- Do NOT restyle to match the sister sites. Kawaii Katz is deliberately its own aesthetic;
  the shared design tokens in `tokens.css` on the other three do **not** apply here.
- Do NOT add product URLs to the sitemap — every product lives on the vendor's own site and
  we should not compete with them for it.
- Do NOT let `/api/` become crawlable; a crawl costs a live scrape of every vendor (§4).
